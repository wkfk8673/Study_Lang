#include "GreedyChange.h"
#include "GreedyMakeOne.h"
#include "GreedyPlusORMultiple.h"
#include "GreedyGuild.h"
#include "GreedyString.h"



int main() {

	/*
	//[그리디 거스름돈]
	GreedyChange change(1260);
	change.calculate();
	change.printResult();

	//[그리디 나누기/빼기 하여 숫자를 1로 만들기]
	GreedyMakeOne number(25, 5);
	number.calculate();
	number.printResult();

	//[그리디 곱하기 / 더하기 선택하여 최대값]
	GreedyPlusORMultiple number2("02984", 0);
	number2.calculate();
	number2.printResult();

	//[그리디 알고리즘 모험가 길드]
	GreedyGuild number3;
	number3.enterData();
	number3.calculate();
	number3.printResult();
   
	//[그리디 문자열 뒤집기]
	GreedyString number4;
	number4.enterData();
	number4.calculate();
	number4.printResult();
	*/


	//[그리디 만들 수 없는 수]
	/*
	int n = 0;
	vector<int> arr;

	cin >> n;

	for (int i = 0; i < n; i++) {
		int x;
		cin >> x;
		arr.push_back(x);
	}

	sort(arr.begin(), arr.end());

	int target = 1;

	for (int i = 0; i < arr.size(); i++) {
		if (target < arr[i]) break;
		target += arr[i];
	}

	cout << target << endl;
	*/





}